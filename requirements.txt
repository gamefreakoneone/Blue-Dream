# Blue-Dream Project Dependencies
# Install PyTorch/Torchvision separately in the conda env before this file:
# conda install pytorch torchvision -c pytorch -c nvidia

# Computer Vision & AI
ultralytics==8.4.99          # YOLO11 model for human detection
opencv-python==5.0.0.93      # Camera capture and video processing

# AI APIs
google-genai==2.12.1               # Gemini API for video analysis
google-auth==2.55.2                 # Firebase Cloud Messaging HTTP v1 service-account auth
google-auth-oauthlib==1.4.0         # Gmail OAuth flow
google-api-python-client==2.198.0   # Gmail API client
openai==2.46.0                      # OpenAI API for transcription and image editing
httpx==0.28.1                       # Async HTTP used by alert delivery and API clients

# Audio Processing
PyAudio==0.2.14       # Audio recording
pydub==0.25.1        # Audio format conversion (requires FFmpeg)

# Utilities
pydantic==2.13.4      # Shared response models and canonical event schemas
Pillow==11.1.0        # Server-side screenshot bounding-box rendering
tzdata==2026.3        # IANA timezone database fallback for Windows

# Web Server
fastapi==0.139.2      # Backend API
uvicorn==0.51.0       # ASGI Server

# Database
motor==3.7.1          # Async MongoDB driver
chromadb==1.5.9       # Persistent vector index for semantic memory search

# Test
pytest==9.1.1
